svnlite status $(dirname $0) | grep ^'?' | sed 's/?  *//' | xargs svnlite add;
svnlite status $(dirname $0) | grep ^'!' | sed 's/!  *//' | xargs svnlite del;
svnlite commit -m "Update `date +%d-%h-%Y_%H-%M`" $(dirname $0);
svnlite update $(dirname $0);
svnlite status -uv $(dirname $0);
